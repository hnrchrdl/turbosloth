var komponist = require('komponist');

module.exports = {
  checkURL: checkURL,
  save: save,
  getList: getList,
  select: select
}


////////////////////////////////////////////////


function checkURL(url) {
  return true;
}

function save(url, port, password, sessionID) {
}

function getList(userID) {
}

function select(userID, namespace) {
}
